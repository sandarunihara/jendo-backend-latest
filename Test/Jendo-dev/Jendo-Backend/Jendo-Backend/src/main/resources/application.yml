spring:
  application:
    name: jendo-app

  datasource:
    url: jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:jendo}?sslmode=disable
    username: postgres
    password: jendo123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  mail:
    host: smtp.mailersend.net
    port: 2525
    username: MS_YQu52G@test-68zxl270qpk4j905.mlsender.net
    password: mssp.O8er9bB.351ndgwdv3qlzqx8.3pNoFSY
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8
    from: MS_YQu52G@test-68zxl270qpk4j905.mlsender.net

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:jendo-app-secret-key-for-jwt-authentication-must-be-at-least-64-characters-long-for-security}
  expiration: 86400000
  refresh-expiration: 604800000

google:
  client-id: ${GOOGLE_CLIENT_ID:}

openrouter:
  api:
    key: ${OPENROUTER_API_KEY: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}

groq:
  api:
    key: ${GROQ_API_KEY:}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

logging:
  level:
    root: INFO
    com.jendo.app: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
  file:
    name: logs/jendo-app.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"